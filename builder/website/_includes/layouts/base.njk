<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title or metadata.title }}</title>
    <meta name="description" content="{{ description or metadata.description }}">
    <link rel="stylesheet" href="{{ '/css/index.css' | url }}">
    <link rel="stylesheet" href="{{ '/css/plyr.css' | url }}">
    <link rel="stylesheet" href="{{ '/css/98.css' | url }}">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.3/css/bulma.min.css">
    <link rel="alternate" href="{{ metadata.feed.path | url }}" type="application/atom+xml" title="{{ metadata.title }}">
    <link rel="alternate" href="{{ metadata.jsonfeed.path | url }}" type="application/json" title="{{ metadata.title }}">
    <script src="https://unpkg.com/stimulus/dist/stimulus.umd.js"></script>
      <script>
      (() => {
        const application = Stimulus.Application.start()

        application.register("fp", class extends Stimulus.Controller {
          static get targets() {
            return [ "fap", "close", "window" ]
          }

          connect() {
          }

          expand() {
            console.log(this.windowTarget)
            this.windowTarget.classList.toggle('is-active');
          }
          
        })
      })()
    </script>
  </head>
  <body data-controller="fp">
    <div class="container">
    <header>
<!--       <h1 class="home"><a href="{{ '/' | url }}">{{ metadata.title }}</a></h1>
      <ul class="nav">
      {%- for entry in collections.all | eleventyNavigation %}
        <li class="nav-item{% if entry.url == page.url %} nav-item-active{% endif %}"><a href="{{ entry.url | url }}">{{ entry.title }}</a></li>
      {%- endfor %}
      </ul> -->

    </header>

  <article class="mt-5 message is-danger">
    <div class="message-body">
      ProjektMelody Chaturbate VOD Archive. For Adults Only.
    </div>
  </article>

    {{ content | safe }}



    <div data-fp-target="window" class="window" style="width: 400px;">
      <div class="title-bar">
        <div class="title-bar-text">🔞💦 Future Porn</div>
        <div class="title-bar-controls">
          <button aria-label="Minimize"></button>
          <button aria-label="Maximize"></button>
          <button data-fp-target="close" aria-label="Close"></button>
        </div>
      </div>
      <div class="window-body">
  
        <article role="tabpanel" id="music">
            <p>A <a href="https://twitter.com/ProjektMelody">ProjektMelody</a> Fan Site</p>
            <fieldset>
              <legend>ProjektMelody New Chaturbate VODs</legend>
              
            </fieldset>
            <section class="field-row">
              <label><span>Website by <a href="https://twitter.com/CJ_Clippy">CJ_Clippy 🥗</a></span></label>
            </section>
          </article>
    
        <section class="field-row" style="justify-content: flex-end">
          <button>OK</button>
          <button>Cancel</button>
        </section>
      </div>




    </div>


    <div data-controller="menu" class="taskbar">
      <div class="start">
        <button data-fp-target="fap" data-action="click->fp#expand" class="button">🔞💦 Fap</button>
      </div>
    </div>

    <footer></footer>

    <!-- Current page: {{ page.url | url }} -->
    <script src="/js/plyr.min.js"></script>
    <script>
      const player = new Plyr('#player');
    </script>
  </div>
  </body>
</html>
